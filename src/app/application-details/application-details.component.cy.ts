import { ApplicationDetailsComponent } from './application-details.component'

describe('ApplicationDetailsComponent', () => {
  it('should mount', () => {
    cy.mount(ApplicationDetailsComponent)
  })
})